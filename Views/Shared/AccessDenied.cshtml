@{
    ViewData["Title"] = "Acceso denegado";
}

<div class="text-center mt-5">
    <h1 class="text-danger"><i class="bi bi-lock-fill"></i> Acceso denegado</h1>
    <p class="lead">No tiene permisos para acceder a esta página.</p>
    <a asp-controller="Home" asp-action="Index" class="btn btn-primary mt-3">Volver al inicio</a>
</div>
