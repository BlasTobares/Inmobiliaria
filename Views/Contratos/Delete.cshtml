@model Inmobiliaria.Models.Contrato
@{
    ViewData["Title"] = "Eliminar Contrato";
}

<h1>Eliminar Contrato</h1>

<h3>¿Seguro que deseas eliminar este contrato?</h3>

<div>
    <strong>@Model.InmuebleDireccion</strong> - @Model.InquilinoNombre
    <br />
    Desde: @Model.FechaInicio.ToString("dd/MM/yyyy") Hasta: @Model.FechaFin.ToString("dd/MM/yyyy")
</div>

<form asp-action="Delete" method="post">
    <input type="hidden" asp-for="Id" />
    <button type="submit" class="btn btn-danger">Eliminar</button>
    <a asp-action="Index" class="btn btn-secondary">Cancelar</a>
</form>

